import './globals.css';

export const metadata = {
  title: 'Atlas AI — Research Terminal',
  description: 'A private AI industrial intelligence research terminal.'
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
